Test the execution policy safely without running destructive commands. Use read-only inspection of `src/policy.zig` only: search for `catastrophic_patterns`, `readonly_cmds`, `evaluate`, and `evaluateTool`. Do not inspect `playground/logs/audit.jsonl`. Finish with a concise explanation of why the dangerous root deletion pattern is blocked and how `guarded` differs from `readonly`.
